/* MDX Snippet:
```js my-file.js
function lorem(ipsum, dolor = 1) {
  const sit = ipsum == null ? 0 : ipsum.sit
  dolor = sit - amet(dolor)
  return sit ? consectetur(ipsum) : []
}
```
*/

// The filename is read from the code block meta (e.g. ```js my-file.js)
// and displayed in code.tsx via {highlighted.meta}.
// No extra component needed — the injection updates code.tsx directly.
